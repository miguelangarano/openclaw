services:
  chrome-cdp:
    build: .
    environment:
      CDP_PORT: 41273
    shm_size: 1gb
    volumes:
      - chrome_data:/data
    ports:
      - "41273:41273"
    restart: unless-stopped

volumes:
  chrome_data:
